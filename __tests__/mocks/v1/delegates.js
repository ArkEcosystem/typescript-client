module.exports = (nock, host) => {
  nock(host)
    .persist()
    .get('/api/delegates')
    .reply(200, { data: [] })
    .get('/api/delegates/count')
    .reply(200, { data: [] })
    .get('/api/delegates/fee')
    .reply(200, { data: [] })
    .get('/api/delegates/forging/getForgedByAccount')
    .query({ generatorPublicKey: 123 })
    .reply(200, { data: [] })
    .get('/api/delegates/get')
    .query({ id: 123 })
    .reply(200, { data: [] })
    .get('/api/delegates/search')
    .reply(200, { data: [] })
    .get('/api/delegates/voters')
    .query({ publicKey: 123 })
    .reply(200, { data: [] })
}
