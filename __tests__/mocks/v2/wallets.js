module.exports = (nock, host) => {
  nock(host)
    .persist()
    .get('/api/wallets')
    .reply(200, { data: [] })
    .get('/api/wallets/top')
    .reply(200, { data: [] })
    .get('/api/wallets/123')
    .reply(200, { data: [] })
    .get('/api/wallets/123/transactions')
    .reply(200, { data: [] })
    .get('/api/wallets/123/transactions/sent')
    .reply(200, { data: [] })
    .get('/api/wallets/123/transactions/received')
    .reply(200, { data: [] })
    .get('/api/wallets/123/votes')
    .reply(200, { data: [] })
    .post('/api/wallets/search')
    .reply(200, { data: [] })
}
