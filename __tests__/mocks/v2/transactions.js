module.exports = (nock, host) => {
  nock(host)
    .persist()
    .get('/api/transactions')
    .reply(200, { data: [] })
    .post('/api/transactions')
    .reply(200, { data: [] })
    .get('/api/transactions/123')
    .reply(200, { data: [] })
    .get('/api/transactions/unconfirmed')
    .reply(200, { data: [] })
    .get('/api/transactions/unconfirmed/123')
    .reply(200, { data: [] })
    .post('/api/transactions/search')
    .reply(200, { data: [] })
    .get('/api/transactions/types')
    .reply(200, { data: [] })
    .get('/api/transactions/fees')
    .reply(200, { data: [] })
}
